#include <iostream>

#include <my_http_parser/client.h>

int main() {
    std::cout << "Hello, World!" << std::endl;
    //Create a client allowing to do request on certain URL (see the PDF at the root)
    return 0;
}
