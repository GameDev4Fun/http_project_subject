#include <iostream>

#include <my_http_parser/server.h>

int main() {
    std::cout << "Hello, World!" << std::endl;
    //Create a server serving HTTP content (see the PDF for more infos)
    return 0;
}
